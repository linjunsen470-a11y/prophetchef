# Claude / Agent Notes

See `AGENTS.md` in this directory and `../AGENTS.MD` at the repository root.

## Short Version

- This is the active Next.js frontend.
- Preserve the existing B2B industrial design.
- Do not bulk-convert or bulk-delete global CSS.
- Keep Sanity Studio changes in `../studio-prophetchef`.
- Run `npm run lint` and `npm run build` before finalizing meaningful changes.
- Use browser verification for UI changes.
